<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        /* Set a light chocolate background color for the body */
        body {
            background-color: #D2B48C;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            color: #333;
        }

        /* Style the navbar */
        .navbar {
            display: flex;
            justify-content: flex-end;
            padding: 10px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .navbar a {
            margin-left: 20px;
            text-decoration: none;
            color: #333;
            font-weight: bold;
        }

        .navbar a:hover {
            color: #4CAF50;
        }

        /* Center the content */
        .container {
            text-align: center;
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: 50px auto;
        }

        /* Style the header */
        h1, h2 {
            margin: 10px 0;
        }

        /* Style the form elements */
        form {
            margin: 20px 0;
        }

        label, input, button {
            display: block;
            width: 100%;
            margin: 10px 0;
        }

        input {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #4CAF50;
            color: white;
            font-size: 16px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

        /* Style the list of topics */
        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            background: #fff;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        li button {
            padding: 5px 10px;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <a href="/about">About us</a>
    </div>
    <div class="container">
        <h1>Dashboard</h1>
        <form action="/dashboard/topic" method="post">
            <label for="topic">Create new topic:</label>
            <input type="text" id="topic" name="topic" required>
            <button type="submit">Create</button>
        </form>
        <h2>Your Contacts</h2>
        <ul>
            <% topics.forEach(topic => { %>
                <li><%= topic.topic %> 
                    <a href="/chat/<%= topic._id %>"><button>Go to chat</button></a>
                </li>
            <% }); %>
        </ul>
    </div>
</body>
</html>
